all:
  hosts:
    boxed-bots:
  vars:
    vault_path_inventory: boxed-bots/data/main,inventory-secrets.yaml
    swarm_manager_host:   boxed-bots
    
    home: "/home/{{ ansible_user }}"
    letsencrypt_path: "/etc/letsencrypt"

managers:
  hosts:
    boxed-bots:
    